# Copy this file to .env.local and update with your values

# OpenAI API Key for content generation
OPENAI_API_KEY=your_openai_key_here

# PostgreSQL Database URL
# Option 1: Remote VPS connection (NOT RECOMMENDED - security risk)
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@YOUR_VPS_IP:5432/persimmon_demo

# Option 2: SSH Tunnel to VPS (RECOMMENDED - secure)
# First run: ssh -L 5432:localhost:5432 root@YOUR_VPS_IP
# Then use:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/persimmon_demo

# Option 3: Local Docker PostgreSQL (for testing)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/persimmon_demo

# n8n Webhook URL (for triggering workflows)
# For production (VPS):
# N8N_WEBHOOK_URL=https://n8n.persimmonlabs.cc/webhook/demo-generated

# For local development (if n8n running locally):
N8N_WEBHOOK_URL=http://localhost:5678/webhook/demo-generated

# Public URL for shareable links
NEXT_PUBLIC_URL=http://localhost:3000

# Optional: Email service (if not using n8n for emails)
# RESEND_API_KEY=re_...